{
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "TA_VERSION='${nextRelease.version}' APP_PACKAGE_FILENAME='TA_CTIS_TAXII_${nextRelease.version}.tar.gz' ./install_deps_and_package.sh"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          { "path": "TA_CTIS_TAXII_${nextRelease.version}.tar.gz", "label": "App Build" }
        ]
      }
    ]
  ]
}
